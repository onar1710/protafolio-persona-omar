---
import Layout from '~/layouts-en/Layout.astro';
import { ArrowRight, ExternalLink, Clock, TrendingUp, Users, CheckCircle, Globe, BarChart, Award } from 'lucide-react';

const project = {
  title: 'Ferrarycucina',
  client: 'Ferrarycucina',
  category: 'Home Services',
  location: 'Bogotá, Colombia',
  duration: '2 weeks',
  technologies: ['Next.js', 'Tailwind CSS', 'Local SEO', 'Portfolio Design'],
  image: '/images/proyecto-imagen-ferrarycucina.png',
  url: 'https://ferrarycucina.com/',
  description: 'Flagship remodeling project with visual portfolio that generates 4-5 weekly quotes in Bogotá.',
  challenge: 'Ferrarycucina needed a stunning visual portfolio to showcase their high-end remodeling work and attract qualified leads in Bogotá\'s competitive market.',
  solution: 'I created a visually impressive portfolio website with optimized image galleries, project showcases, and local SEO to dominate Bogotá remodeling searches.',
  results: [
    { metric: '4-5 quotes/week', description: 'Consistent lead generation' },
    { metric: 'First page Google', description: 'Local remodeling keywords' },
    { metric: '50+ projects', description: 'Portfolio showcase' },
    { metric: '95% mobile', description: 'Mobile optimization score' }
  ]
};
---

<Layout title="Ferrarycucina Case Study - Remodeling Portfolio Website | Omar Fuentes" description="Flagship remodeling portfolio website for Ferrarycucina in Bogotá. Next.js development with local SEO optimization generating 4-5 weekly quotes and first-page Google rankings." keywords="remodeling portfolio bogota, home services website design, kitchen remodeling website, local seo bogota, ferrarycucina case study, next.js portfolio website, remodeling lead generation, bogota web development">
  <div class="max-w-7xl mx-auto px-6 pt-32 pb-20">
    <!-- Hero Section -->
    <div class="text-center mb-20">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 text-primary border border-primary/20 mb-6">
        <Globe className="w-4 h-4" />
        <span class="text-sm font-medium">{project.location}</span>
      </div>
      <h1 class="text-4xl md:text-6xl font-bold mb-6">
        {project.title}
      </h1>
      <p class="text-xl text-muted-foreground max-w-3xl mx-auto mb-8">
        {project.description}
      </p>
      <div class="flex flex-wrap gap-4 justify-center mb-8">
        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-blue-500/10 text-blue-700 dark:text-blue-300 border border-blue-500/20">
          {project.category}
        </span>
        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-500/10 text-green-700 dark:text-green-300 border border-green-500/20">
          <Clock className="w-4 h-4 mr-1" /> {project.duration}
        </span>
      </div>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href={project.url}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 px-8 py-4 bg-primary text-black rounded-xl hover:bg-primary/90 transition-all text-lg font-semibold shadow-lg shadow-primary/20"
        >
          <ExternalLink className="w-5 h-5" />
          Visit Website
        </a>
        <a 
          href="/en/contact"
          class="inline-flex items-center gap-2 px-8 py-4 border border-primary text-primary rounded-xl hover:bg-primary/10 transition-all text-lg font-semibold"
        >
          <ArrowRight className="w-5 h-5" />
          Get Similar Project
        </a>
      </div>
    </div>

    <!-- Project Image -->
    <div class="mb-20">
      <div class="relative rounded-3xl overflow-hidden border border-black/5 dark:border-white/10">
        <img 
          src={project.image}
          alt={project.title}
          width="1200"
          height="800"
          class="w-full h-auto object-cover"
          loading="lazy"
          decoding="async"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent"></div>
        <div class="absolute bottom-8 left-8 text-white">
          <div class="text-sm opacity-80 mb-2">Live Project</div>
          <div class="text-2xl font-bold">{project.title}</div>
        </div>
      </div>
    </div>

    <!-- Project Details -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-12 mb-20">
      <div class="lg:col-span-2">
        <h2 class="text-3xl font-bold mb-8">Project Overview</h2>
        
        <div class="mb-12">
          <h3 class="text-xl font-semibold mb-4">The Challenge</h3>
          <p class="text-muted-foreground leading-relaxed">
            {project.challenge}
          </p>
        </div>

        <div class="mb-12">
          <h3 class="text-xl font-semibold mb-4">The Solution</h3>
          <p class="text-muted-foreground leading-relaxed mb-6">
            {project.solution}
          </p>
          <ul class="space-y-3">
            <li class="flex items-start gap-3">
              <CheckCircle className="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" />
              <span class="text-muted-foreground">Stunning visual portfolio with high-quality image galleries</span>
            </li>
            <li class="flex items-start gap-3">
              <CheckCircle className="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" />
              <span class="text-muted-foreground">Local SEO optimization for Bogotá remodeling searches</span>
            </li>
            <li class="flex items-start gap-3">
              <CheckCircle className="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" />
              <span class="text-muted-foreground">Project showcase with before/after galleries</span>
            </li>
            <li class="flex items-start gap-3">
              <CheckCircle className="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" />
              <span class="text-muted-foreground">Contact forms optimized for lead generation</span>
            </li>
          </ul>
        </div>

        <div>
          <h3 class="text-xl font-semibold mb-4">Technologies Used</h3>
          <div class="flex flex-wrap gap-2">
            {project.technologies.map(tech => (
              <span class="px-3 py-1 rounded-full bg-primary/10 text-primary border border-primary/20 text-sm">
                {tech}
              </span>
            ))}
          </div>
        </div>
      </div>

      <div>
        <h2 class="text-3xl font-bold mb-8">Results</h2>
        <div class="space-y-6">
          {project.results.map((result, index) => (
            <div key={index} class="p-6 rounded-2xl border border-black/5 dark:border-white/10 bg-black/5 dark:bg-white/5">
              <div class="text-2xl font-bold text-primary mb-2">{result.metric}</div>
              <div class="text-muted-foreground">{result.description}</div>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- CTA Section -->
    <div class="text-center p-12 rounded-2xl border border-foreground/10 bg-gradient-to-br from-primary/5 to-primary/10">
      <h2 class="text-3xl font-bold mb-4">Need a Portfolio Website?</h2>
      <p class="text-muted-foreground max-w-2xl mx-auto mb-8">
        I can create a stunning portfolio website that showcases your work and generates consistent leads for your business.
      </p>
      <a 
        href="/en/contact"
        class="inline-flex items-center gap-2 px-8 py-4 bg-primary text-black rounded-xl hover:bg-primary/90 transition-all text-lg font-semibold shadow-lg shadow-primary/20"
      >
        <ArrowRight className="w-5 h-5" />
        Build Your Portfolio
      </a>
    </div>
  </div>
</Layout>
